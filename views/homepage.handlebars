{{!-- We need to make this our own; from MVC mini project --}}
<!-- Loop over each object in the projects array -->
{{#each questions as |question| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/project/{{question.id}}">{{question.content}} {{question.topic}}</a>
    </h2>
    <p>
      {{answer.content}}
    </p>
    {{!-- <p>Created by {{user.userName}} on {{format_date question.date_created}}</p> --}}
  </div>
  <div class="col-md-7">
  </div>
</div>
{{/each}}
