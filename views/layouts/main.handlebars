{{!-- we need to make this our own; from MVC mini project --}}
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QTips</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

</head>

<body>
  <div class="flex-column justify-space-around">



    
    {{!-- <header class="display-flex justify-space-between align-center p-2">
      <h1 class= 'row d-flex justify-content-between'>
        {{#if logged_in}}
          <a class = 'col-md-10' href="/" style="color: white">Coding QTips -- Quizzes and Tips</a>
          <button class="no-button col-md-2" id="logout">Logout</button> 
        {{else}}
          <a class = 'col-md-10' href="/" style="color: white">Coding QTips -- Quizzes and Tips</a>
          <a class = 'col-md-2 d-flex align-items-center' href="/login" style="font-size:large">Login</a>
        {{/if}}
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <div class = 'row d-flex justify-content-around'>
          {{!-- <a href="/profile" style="color: white">Profile</a>  --}}
          {{!-- <button class="no-button col-md-4" id="logout">Logout</button> --}}
          <a class = 'col-md-4' href="/api/question">add a Question</a>
          <a class = 'col-md-4' href="/api/tip">add a Tip</a>
        </div>
        {{else}}
        <div class = 'row d-flex justify-content-around'>
          {{!-- <a class = 'col-md-4' href="/login">Login</a> --}}
          <a class = 'col-md-4' href="/api/question">add a Question</a>
          <a class = 'col-md-4' href="/api/tip">add a Tip</a>
        </div>
        {{/if}}
      </nav>

    </header> --}}

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
